# TODO TerrOir

Priorités forward-looking uniquement. Pour l'historique complet des commits / chantiers clos, voir [`CHANGELOG.md`](./CHANGELOG.md). Pour les leçons apprises / pitfalls thématiques, voir [`LESSONS.md`](./LESSONS.md).

> 🚀 **Pré-Live** : la checklist consolidée P0/P1/P2 des bloquants ouverture publique est désormais [`docs/runbooks/checklist-pre-live-2026-05-06.md`](./runbooks/checklist-pre-live-2026-05-06.md) (T-244 + T-257 + T-261). À utiliser comme source de vérité unique au moment du go-live. Les items détaillés ci-dessous restent la source d'origine des descriptions, la checklist est une vue synthétisée.

> **Convention numérotation T-XXX** (instaurée 28/04/2026) — chaque item porte un identifiant stable permettant le référencement (« supprime T-005 », « reflag T-010 »). Numérotation par catégorie avec espaces réservés pour insertion future :
> - `T-001` → `T-009` : Bloquants lancement
> - `T-010` → `T-019` : Bugs et incidents identifiés
> - `T-040` → `T-079` : Externes / config
> - `T-080` → `T-099` : Audit logs (chantier dédié futur)
> - `T-100` → `T-149` : Chantiers code futurs
> - `T-150` → `T-179` : Investigations produit à trancher
> - `T-200` → `T-299` : Roadmap produit (HAUTE / MOYENNE / BASSE)
> - `T-300` → `T-399` : Vision funnel producteur Phase 3

## 🟠 En cours

- **Chantier "Notre démarche" — page pédagogique GMS** (refonte item roadmap "Prix GMS sur chaque fiche produit" décidée session 28/04 après-midi).
  - ✅ **Phase A livrée 28/04** (PR #2 + extension dotenv PR #6) — DB tables `gms_prices` + `gms_prices_history` + RLS public read + 10 références seed initial (4 bovin + 3 porcin + 3 ovin) + helper `lib/gms-prices/fetch-active.ts` + 9 tests vitest. Migration `20260428000000_gms_prices` apply confirmée prod, seed apply confirmée prod (10 références actives, breakdown filière OK).
  - 🔲 **Phase B à venir** — Interface admin `/admin/gms-prices` (CRUD références + workflow update mensuel via RPC INSERT history + UPDATE live en transaction).
  - 🔲 **Phase C à venir** — Page publique `/notre-demarche` (graphique circuit interactif `<CircuitVisualizer>` 8 maillons GMS / 5 maillons TerrOir avec désactivation maillon par maillon + redistribution éleveur + tooltip sources, tableau comparaison 10 références, hero chiffre choc, encart home teaser, navbar primaire). **Full Claude Code** (Claude Design indispo cette session — itération visuelle CD repassera plus tard pour polish). Décisions tranchées : slug `/notre-demarche`, données graphique placeholder à calibrer plus tard sur sources OFPM/Idele/CGAAER, item navbar primaire, encart home entre Steps et Products grid.

## 🔴 Bugs ouverts
- **T-011 Bug intermittent navbar « Connexion » affiché loggé** — fix sync `useEffect` `UserProvider` PR #14 résout la majorité des cas, mais signalé comme parfois intermittent en prod. Mitigation préparée : bonus défensif `INITIAL_SESSION` protect dans `UserProvider` (snippet rédigé en PR #14 comme réserve, à activer si re-flag persistant).


## 🔴 À faire (bloquants lancement)

- **T-001 Onboarder Julien (GAEC du Rheu)** — pages landing Stripe Connect `/connect/done` + `/connect/refresh` désormais en place (commit `e93043e`), mais onboarding end-to-end Stripe Live pas encore testé en situation réelle. À garder bloquant tant que le flow n'est pas validé avec un vrai producer.
- **T-002 Basculer Stripe en mode Live** (aujourd'hui en Test). Au moment de la bascule, créer un nouveau webhook endpoint dans Stripe Dashboard pointant sur `https://www.terroir-local.fr/api/stripe/webhook` en mode Live (le webhook actuel est en mode Test).

## 🔐 Avant lancement public

**T-003 Audit tech externe pré-lancement** (~2-4 k€, 1-2 semaines) :

- Pentest complet de l'application
- Review des policies RLS Supabase (toutes les tables)
- Review des server actions sensibles : checkout Stripe, paiements, RGPD, invitation admin
- Review du webhook Stripe et flows de paiement
- Audit des flows Stripe Customer + Connect (commission, payouts)
- Review de la conformité RGPD (registre, consentements, droits)
- Tests de charge sur endpoints critiques (`create-payment-intent`, `create-order-with-items` RPC, `search_producers`)
- Vérification absence d'injections SQL latentes
- **Audit npm vulnérabilités pré-existantes** (5 vulnerabilities détectées 28/04 sur le repo : 1 critical + 3 high + 1 moderate, indépendantes de tout chantier session). Lancer `npm audit` détaillé puis traiter en chantier dédié + revue manuelle des breaking changes potentiels avant `npm audit fix --force`.
- **Validation juridique page `/notre-demarche`** (livraison Phase C ultérieure) — avocat spécialisé droit de la concurrence/conso à embarquer pour wording exact + disclaimers + représentation visuelle des marges intermédiaires (risque dénigrement implicite). Pattern défensif déjà cadré (pas de mention nominale concurrent, source FranceAgriMer/OFPM citée systématiquement, mise en contexte qualité, pas de "Économisez X€"), à valider en audit.

À déclencher avant le go-live public (avant premiers clients payants). Prévoir avant la bascule Stripe Test → Live.

## 🟡 À faire (non bloquants)

### Externes / config

- **T-040 Twilio SMS** : numéro FR à régler.
- **T-041 Pages légales (Mentions légales / CGU / CGV / Politique de confidentialité) — bloqueur lancement public** — fusion T-041 (mentions légales footer pro href mort) + T-046 (mentions légales footer consumer affichées « à venir » italique muted, refonte 27/04 Claude Design) + finding audit Auth #1 séance 29/04. Action externe Romain (rédaction + validation juridique avocat avant go-live public, risque CNIL + DGCCRF si pages bidon). Templates Next.js pages `/mentions-legales` + `/cgu` + `/cgv` + `/politique-confidentialite` + footer enrichi liens (consumer + producer) + checkbox signup acceptation CGU/RGPD + bandeau cookies basique conformité ePrivacy. Une fois contenus juridiques disponibles, chantier code dédié.
- **T-042 SMTP custom Supabase Resend à configurer (recommandé avant lancement)** — observation récente : mails Auth atterrissant en spam. Configurer Resend en SMTP custom (rate limit Supabase built-in ~3-4/h, non destiné à la production) serait propre. Action externe Romain via Dashboard.
- **T-043 Templates Supabase Auth Email — validation visuelle complète** — Magic Link template à mettre à jour avec `{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=magiclink` (action Romain post-PKCE Option B, commit `09c219d`). Reset Password template à mettre à jour avec `${SITE_URL}/reinitialiser-mot-de-passe?token_hash={{ .TokenHash }}&type=recovery` (action Romain post-`5ff9394`). Confirm Signup, Change Email, Invite User pas testés visuellement (rendus mais flow end-to-end non validé). Action externe Romain via Dashboard.
- **T-044 Branding Stripe Connect** — flag pendant la session 27/04 pour ne pas mélanger avec les bugs critiques. Investigation dédiée future avec accès doc Stripe à jour (logo, couleurs, pages `/connect/*`, branding marketplace).
- **T-045 Webhook Stripe mode Live** — créer un nouveau webhook endpoint dans Stripe Dashboard pointant sur `https://www.terroir-local.fr/api/stripe/webhook` au moment de la bascule Test → Live. Mode Test confirmé déjà OK (validation 27/04 matin).
- **T-046 HIBP password protection (Have I Been Pwned) — bloqueur lancement** — finding audit Auth #1 séance 29/04, bloqué Pro plan Supabase. Action externe Romain : upgrade Supabase Pro plan (25$/mois) puis toggle Dashboard Settings → Auth → Password Strength → « Enable HIBP password check » (vérifie passwords signup/reset contre bases breach connues, bloque passwords compromis même si fort en complexité). Pas de chantier code TerrOir nécessaire. À traiter au moment de l'upgrade Pro plan pour autres features (read replicas, point-in-time recovery).

### Audit logs

- **T-080 UI admin pour `audit_logs`** — créer une page back-office `/admin/audit-logs` avec filtres par `event_type`, `user_id`, date range, pagination. La table est alimentée par 13 event types (5 auth Phase 1 + 6 payment Phase 2 + 2 retry refund Phase 2bis 28/04) — voir `CHANGELOG.md`.
- **T-081 Events audit Phase 3 — `[ADMIN_INVITE_*]` structuré** ✅ **LIVRÉE 2026-05-03** — Phase 3 finale câble les 5 events restants : cluster `admin_invite_*` (`admin_invite_sent`, `admin_invite_draft_resend`, `admin_invite_blocked_admin`, `admin_invite_blocked_producer`, `admin_invite_expired` sur 4 server actions de claim). Voir `CHANGELOG.md`. Phases 1 (auth, 5 events), 2 (payment, 6 events), 2bis (retry refund, 2 events) et 3 PR-A (T-081 PR-A + T-307/T-309/T-310, 6 events) déjà livrées antérieurement.
- **T-082 [Conformité] Documenter durée de rétention `audit_logs` cluster `admin_invite_*`** (issu rapport conformité comité T-081 round 2, 03/05/2026) — les events `admin_invite_*` (sent, draft_resend, blocked_admin, blocked_producer, expired) contiennent l'email du destinataire, donnée personnelle même en contexte B2B producteur. Base légale plausible : intérêt légitime (sécurité plateforme + traçabilité actions admin). Durée de conservation à fixer (typique 12-36 mois pour logs admin marketplace) et à inscrire dans le registre des traitements + politique de confidentialité producer avant le passage en Live. Non-bloquant T-081, prérequis go-live public. Chantier d'origine : T-081.
- **T-083 [Sécurité] Rate-limit ou masquage si l'UI admin `/audit-logs` expose un filtre par email saisi côté client** (issu rapport sécurité comité T-081 round 2, 03/05/2026) — risque d'oracle d'énumération de comptes : un admin (ou un compte admin compromis) pourrait scanner si un email est connu de la plateforme via les events `admin_invite_blocked_*`. À cadrer au moment du chantier T-080 (UI admin audit_logs) : soit rate-limit sur la recherche par email, soit masquage partiel des emails dans les events `admin_invite_*` côté UI, soit RLS stricte revérifiée + journalisation des accès au journal lui-même. Chantier d'origine : T-081.
- **T-084 [Produit] Vérifier libellés humains UI `/audit-logs` pour les 5 nouveaux events `admin_invite_*`** (issu rapport produit comité T-081 round 2, 03/05/2026) — ex. « Invitation envoyée », « Relance d'invitation », « Blocage : email déjà admin », « Blocage : producteur déjà inscrit », « Lien expiré cliqué » plutôt que les identifiants techniques bruts. Si la traduction est centralisée (map côté UI) et déjà en place pour les 13 events antérieurs, rien à faire — sinon, passe de wording à prévoir au moment du chantier T-080. Chantier d'origine : T-081.
- **T-085 [Business / NICE-TO-HAVE] Dashboard « taux de conversion invitation → onboarding complet »** (issu rapport business comité T-081 round 2, 03/05/2026) — exploiter les events `admin_invite_*` désormais captés par T-081 pour produire une métrique B2B utile au pilotage du recrutement producteurs au moment de l'ouverture Pays de la Loire. La donnée est en base, aucun dashboard prévu côté admin. Pas un blocage, à reprendre au moment de scaler le recrutement producteur. Chantier d'origine : T-081.

### Chantiers code futurs

- **T-104 Anomalie traçabilité migration `webhook_events_processed`** — la table existe en base (créée 29/04/2026 dans le cadre du chantier dédup webhooks T-103) mais l'historique `supabase_migrations.schema_migrations` ne contient pas l'entrée correspondante au timestamp `20260429000000`. La migration a été appliquée hors workflow standard MCP. Pas de bug fonctionnel, juste un trou de traçabilité côté Supabase. À régler par un INSERT ciblé dans `schema_migrations` (ou re-application idempotente) lors d'une prochaine session de maintenance DB. Pas critique.
- **T-105 Flux invitation : cas « email déjà en base » à détecter proprement côté UX** (au-delà de la correction fonctionnelle du Chantier 2).
- **T-107 Instrumentation `*_refund_failed` audit_logs sur paths refund admin manuel + cron order-timeout** (pré-requis avant extension du cron retry-failed-refunds aux 3 paths). Aujourd'hui seul le path résurrection bloquée P1 robuste pose `order_revival_refund_failed` (chantier P1 robuste 27/04). Les 2 autres paths refund (`/api/stripe/refund` admin manuel, cron `order-timeout`) ne posent aucun event audit `*_refund_failed` exploitable. Pré-requis avant extension du cron retry-failed-refunds (PR #5 mergée 28/04, scope minimal résurrection bloquée only). Chantier dédié futur.
- **T-109 Invalidation auto des invitations actives à chaque nouvel envoi** (R4 du rapport TB invite session 28/04). Migration SQL : à chaque INSERT `producer_invitations`, faire un UPDATE `producer_invitations SET expires_at=now() WHERE email=$1 AND used_at IS NULL AND expires_at > now()`. Optionnellement : ajouter un UNIQUE partial index `(email) WHERE used_at IS NULL AND expires_at > now()`. Inclure migration SQL + adapter route + tests.
- **T-110 Casse email normalisée `ilike` sur tous les lookups admin_users / users / producers / producer_interests** (chantier transversal détecté pendant inspection TB invite 28/04). Aujourd'hui certaines routes font `eq("email", input.email)` (case-sensitive) alors que `producer_interests` utilise déjà `ilike`. Si l'admin saisit `Bob@example.fr` mais `users.email='bob@example.fr'`, les pré-checks foirent silencieusement. À normaliser : soit `ilike` partout, soit `lower` au save côté DB via trigger. Audit nécessaire de tous les call sites email-keyed dans le repo.
- **T-111 Design system — Phase 2 (extension)** — une fois la home consumer refondue stabilisée (Phase 1 livrée par session 27/04), étendre la migration design system terra-primary au reste du repo : refonte fiche produit (`/producteurs/[slug]/produits/[id]`), refonte panier + checkout (`/panier`, `/checkout`), refonte UI kit producer (`pro.terroir-local.fr`), refonte UI kit admin (`admin.terroir-local.fr`). Migration variant `accent` (transitionnel green sur call sites admin/producer) → `primary` terra ou `success` green selon sémantique métier. Bundle Claude Design contient des références preview pour ces écrans (cards `metric-tile.html`, `product-card.html`, `dayslots.html`) déjà alignés sur le DS.

#### Follow-ups comité review T-200 (round 1, 03/05/2026)

- **T-201 [Business] Mesure d'usage du widget distance fiche producteur** (issu rapport business comité T-200 round 1, 03/05/2026) — instrumenter le DistanceWidget (taux de clic « Utiliser ma position » vs saisie CP, taux de finalisation, distance médiane affichée). Sans ces métriques, impossible de savoir si l'investissement améliore la conversion ou la perception « circuit court ». Reprendre via l'event tracking déjà en place sur les autres surfaces consumer. Chantier d'origine : T-200.
- **T-202 [Business] Backfill lat/lng des 5 producteurs sans coordonnées avant ouverture publique** (issu rapport business comité T-200 round 1, 03/05/2026) — la moitié des producteurs en prod (5/10) n'ont pas de `latitude`/`longitude` ; sur leurs fiches, le widget distance ne s'affiche pas (early-return ScoreCarbonBlock + DistanceWidget). Bloquant avant ouverture publique : sinon la moitié des fiches perdent leur principal argument différenciant. À faire via géocodage de l'adresse postale existante. Chantier d'origine : T-200.
- **T-203 [Business] Process de relance producteurs déjà onboardés sur les 3 enums score carbone** (issu rapport business comité T-200 round 1, 03/05/2026) — les 3 nouveaux champs `mode_elevage`, `alimentation`, `densite_animale` ne sont alimentés que via l'onboarding (formulaire StepInfos). Les producteurs déjà inscrits avant la livraison T-200 ont leurs colonnes à `null` → bloc « Notre démarche » vide sur leur fiche. Prévoir un email de relance + UI dédiée dans `/ma-page` producteur (édition post-onboarding). Chantier d'origine : T-200.
- **T-204 [Business] Anticiper scaling géocodeur public au-delà de la Sarthe** (issu rapport business comité T-200 round 1, 03/05/2026) — `api-adresse.data.gouv.fr` est un service public sans SLA contractuel. À 1500+ producteurs sur Pays de la Loire puis France, l'API peut devenir un goulot ou rate-limiter TerrOir. Prévoir bascule vers un géocodeur avec SLA OU cache côté serveur des CP les plus fréquents. À cadrer au moment du scaling. Chantier d'origine : T-200.
- **T-205 [Business] Filtres de recherche basés sur les 3 enums score carbone** (issu rapport business comité T-200 round 1, 03/05/2026) — les données collectées (mode élevage, alimentation, densité) sont une mine pour la roadmap produit post-lancement : « voir uniquement les éleveurs plein air », « filtrer par alimentation à l'herbe ». À inscrire dans la roadmap moyen terme. Chantier d'origine : T-200.
- **T-206 [Conformité] Review avocat T-003 — formulation comparative « ~1500 km » et page `/notre-demarche`** (issu rapport conformité comité T-200 round 1, 03/05/2026) — la mention « ~1500 km en moyenne en circuit long » sur DistanceWidget est sur le même terrain juridique que la page `/notre-demarche` (loi Climat & Résilience : encadrement renforcé des allégations environnementales et de la neutralité carbone, sanctions DGCCRF). Source ADEME affichée à côté du chiffre comme garantie d'ancrage factuel. À faire valider par l'avocat dans le cadre de T-003 avant ouverture publique. Chantier d'origine : T-200.
- **T-207 [Conformité] Mise à jour politique de confidentialité avant Live — widget distance et géocodage tiers** (issu rapport conformité comité T-200 round 1, 03/05/2026) — avant le passage en Live, mentionner dans la politique de confidentialité : (a) collecte ponctuelle de géolocalisation côté client (Geolocation API, après action explicite utilisateur), (b) recours à `api-adresse.data.gouv.fr` comme service tiers (sous-traitant de fait, même service public), (c) conservation `sessionStorage` uniquement, jamais persistée côté serveur. Chantier d'origine : T-200. **r4 : la mention textuelle « voir notre politique de confidentialité » a été retirée de `PrivacyNote()` dans `DistanceWidget.tsx` tant que la page n'existe pas (promesse d'un document opposable absent = trompeur). Au moment de la livraison de cette page, réintroduire un `<Link>` cliquable dans `PrivacyNote()` — pointeur de code documenté dans le commentaire au-dessus de la fonction.**
- **T-208 [Conformité] Inscription du widget distance au registre des traitements RGPD** (issu rapport conformité comité T-200 round 1, 03/05/2026) — registre à constituer avant Live. Ajouter une entrée pour le widget distance même si la donnée ne quitte pas le navigateur (obligation d'inventaire des traitements art. 30 RGPD). Chantier d'origine : T-200.
- **T-209 [Conformité] CGU producteur — clause de véracité sur les 3 enums score carbone déclaratifs** (issu rapport conformité comité T-200 round 1, 03/05/2026) — `mode_elevage`, `alimentation`, `densite_animale` sont des allégations producteur (non vérifiées par TerrOir). Prévoir dans les CGU producteur : (a) clause d'engagement sur la véracité des informations saisies, (b) mécanisme de mise à jour / contestation (signalement consumer ?). À cadrer avec les CGU marketplace globales. Chantier d'origine : T-200.
- **T-210 [Conformité] Vérifier non-confusion entre nomenclature T-200 et mentions réglementées** (issu rapport conformité comité T-200 round 1, 03/05/2026) — `plein_air`, `semi_plein_air` peuvent créer une confusion avec des mentions réglementées (Label Rouge, AB, « plein air » au sens du règlement européen CE 543/2008 sur les œufs/volailles), où le terme a une définition légale stricte (densité, accès, etc.). Risque d'allégation trompeuse si un producteur coche `plein_air` sans respecter le cahier des charges réglementaire associé. À arbitrer avec l'avocat dans le cadre de T-003 : disclaimer in situ, renommage des enums (« libre parcours » au lieu de `plein_air` ?), ou intégration des labels réglementés au formulaire. Chantier d'origine : T-200.
- **T-211 [Produit] Repenser le bloc « Notre démarche » modulaire selon le métier producteur** (issu rapport produit comité T-200 round 1, 03/05/2026) — les 3 enums sont taillés pour l'élevage. Pour maraîchers, boulangers, apiculteurs, arboriculteurs : prévoir des indicateurs adaptés au métier (rotation des cultures, traitements phyto, type de levain, traitement varroase, etc.). Aujourd'hui la fiche maraîcher montre uniquement le widget distance + titre adaptatif « Au plus près de chez toi ». À cadrer avec les déclinaisons par `type_production`. Chantier d'origine : T-200.
- **T-212 [Produit] Aperçu visuel temps réel dans l'onboarding « voici comment ça apparaîtra »** (issu rapport produit comité T-200 round 1, 03/05/2026) — aider le producteur à choisir parmi les 3 enums en lui montrant en direct le rendu de la pill colorée + tooltip côté fiche publique. Évite les saisies par défaut ou erronées. À implémenter dans `StepInfos.tsx` + `/ma-page` producteur (édition post-onboarding). Chantier d'origine : T-200.
- **T-213 [Produit] Pré-remplissage global de la position consumer (header / compte)** (issu rapport produit comité T-200 round 1, 03/05/2026) — actuellement la saisie position est demandée sur chaque fiche producteur. Évolution UX : pré-remplir une fois au niveau global (header ou compte consumer), réutiliser sur toutes les fiches. Implique potentiellement de stocker un CP côté `users` (cf. discussion option B vs A pendant le brief T-200) — décision produit à reprendre. Chantier d'origine : T-200.
- **T-214 [Produit] Comparatif GMS visuel post-review juridique T-003** (issu rapport produit comité T-200 round 1, 03/05/2026) — une fois la review avocat T-003/T-206 passée, envisager une version visuelle plus parlante du comparatif 1500 km (silhouette de camion qui parcourt la distance, carte France stylisée, animation au scroll). Ne pas démarrer avant validation juridique de la formulation comparative. Chantier d'origine : T-200.
- **T-215 [Produit] Audit a11y dédié sur les pills colorées du bloc score carbone** (issu rapport produit comité T-200 round 1, 03/05/2026) — vérifier contraste WCAG AA sur les 3 pills (vert/terra/orange) et que l'information n'est pas portée uniquement par la couleur (typique sur le rouge/orange « intensif »). Le hint est désormais affiché en clair sous chaque pill (depuis r2 : `title` natif retiré pour cohérence mobile, l'info reste lisible par les lecteurs d'écran via le DOM standard). Chantier d'origine : T-200.
- **T-216 [Produit] Internationalisation future — adapter « ~1500 km » et `api-adresse.data.gouv.fr` hors France** (issu rapport produit comité T-200 round 1, 03/05/2026) — si TerrOir s'étend hors Sarthe puis hors France, la référence 1500 km (ADEME française) et l'API code postal `data.gouv.fr` (service public français) devront être généralisés. À reprendre au moment de la décision d'expansion. Chantier d'origine : T-200.
- **T-217 [Sécurité] Politique d'arrondi/floutage des coordonnées producteur** ✅ **LIVRÉE 2026-05-06** — Option A retenue (maintien `roundCoord` 2 décimales sur les 4 surfaces publiques). Test contractuel ajouté sur `/producteurs/[slug]` (`tests/app/(public)/producteurs/[slug]/page.test.tsx`) verrouillant la garantie à la frontière Server → Client. Voir `docs/fixes/coords-privacy-policy-2026-05-06.md` pour rationale (B/C écartés), 4 call sites verrouillés, et raison du non-emploi d'un `roundCoord` défensif au niveau page. T-227 (étude ré-identification par croisement) reste **ouvert** — menace dominante = photos+commune, pas GPS arrondi. Backlog defense in depth : T-235 (vue Postgres `producers_public`), T-236 (rate-limit search), T-238 (scan auto anti-fuite). Chantier d'origine : T-200.
- **T-218 [Sécurité] Audit RLS global table `producers` au prochain chantier touchant la table** (issu rapport sécurité comité T-200 round 1, 03/05/2026) — la table accumule les colonnes (T-200 ajoute 3 enums, plusieurs chantiers passés ont ajouté abonnement, badges, score). Revérifier toutes les policies RLS après plusieurs migrations cumulées : `producers admin all`, `producers owner read/insert/update`, `producers public read when public` couvrent toujours l'usage attendu, pas de fuite par jointure ou colonne nouvelle ? Chantier d'origine : T-200.
- **T-219 [Technique] Cache serveur géocodage CP→lat/lng** ✅ **LIVRÉE 2026-05-06** — Option α retenue (table Supabase `public.geocode_cache` + RPC atomiques `bump_geocode_cache` / `upsert_geocode_cache` + route `/api/geocode` rate-limitée Upstash 30/min/IP + helper client `geocodePostalCodeViaApi`). Migration `supabase/migrations/20260506181153_t219_geocode_cache.sql` à appliquer manuellement via Supabase Studio. DistanceWidget bascule sur `/api/geocode` au lieu de `api-adresse.data.gouv.fr` direct. PrivacyNote du widget mis à jour pour refléter la nouvelle chaîne (CP transite via TerrOir, cache anonyme, pas de PII). Continuité T-200 r1 préservée (zéro audit log applicatif, pas de log par-IP côté DB, pas de profilage user) — verrou de tests explicite. T-204 partiellement clôturée (ce cache amortit le scaling, plan B fournisseur T-226 reste ouvert). Voir `docs/fixes/geocode-cache-2026-05-06.md`. Chantier d'origine : T-200.
- **T-220 [Technique] Codegen TS depuis migration SQL pour les enums** (issu rapport technique comité T-200 round 1, 03/05/2026) — au-delà du test Vitest de parité TS↔SQL ajouté en T-200, mettre en place un script de codegen qui génère `*_VALUES` depuis la dernière migration (ou inversement). Évite la dérive structurelle au prochain ajout d'enum (T-300 et au-delà). Chantier d'origine : T-200.
- **T-225 [Technique] Workflow staging→prod pour les migrations Supabase** (issu rapport technique comité T-200 round 1, 03/05/2026) — la pratique actuelle d'appliquer les migrations directement en prod via MCP Supabase est acceptable en pré-lancement (TerrOir n'est pas Live). À reconsidérer avant ouverture publique : monter un projet Supabase staging, workflow standard apply migration → staging → tests → apply prod. Chantier d'origine : T-200.

#### Follow-ups comité review T-200 (round 2, 03/05/2026)

> Rappel r2 : T-202 (backfill lat/lng), T-206 (review avocat « 1500 km ») et T-210 (non-confusion `plein_air` vs règlement européen) sont confirmés **bloquants ouverture publique**. T-206 et T-210 à traiter dans la même vague juridique T-003.

- **T-226 [Business] Plan B fournisseur géocodage (BAN, Google Geocoding payant)** (issu rapport business comité T-200 round 2, 03/05/2026) — `api-adresse.data.gouv.fr` est gratuit aujourd'hui, sans engagement contractuel. Si l'API tombe ou change de politique, prévoir une note d'archi sur le fournisseur de remplacement (BAN auto-hébergée, Google Geocoding payant, MapBox Geocoding). Évite de découvrir le sujet en urgence. Complément du T-204 (anticiper scaling) et T-219 (cache serveur). Chantier d'origine : T-200.
- **T-227 [Sécurité] Étude ré-identification adresse producteur par croisement de données publiques** (issu rapport sécurité comité T-200 round 2, 03/05/2026) — l'arrondi 2 décimales (~1 km) appliqué en r1+r2 réduit la fuite mais ne l'élimine pas : un attaquant qui croise plusieurs fiches producteurs publiques (nom de la ferme, commune, photos, indices contextuels) avec un point GPS arrondi peut potentiellement ré-identifier l'adresse. À documenter dans la politique de privacy producteur avant ouverture publique. À cadrer avec T-217 (politique uniforme de floutage à décider). Chantier d'origine : T-200.
- **T-228 [Sécurité] Audit Stripe metadata pre-Live — pas de fuite des nouveaux champs T-200** (issu rapport sécurité comité T-200 round 2, 03/05/2026) — vérifier au moment du passage Live Stripe que les 3 nouvelles colonnes `mode_elevage`, `alimentation`, `densite_animale` n'ont pas été ajoutées par mégarde à un payload Stripe metadata (payment intent, customer, account). Pas signalé dans la PR T-200 mais à scanner avant Live. Chantier d'origine : T-200.
- **T-229 [Sécurité] Process backfill T-202 sans logging d'adresses précises** (issu rapport sécurité comité T-200 round 2, 03/05/2026) — au moment du backfill lat/lng des 5 producteurs sans coordonnées (T-202), s'assurer que la procédure ne logge pas les adresses précises dans l'historique de migration ou un script versionné en repo. Stratégie attendue : migration data avec valeurs littérales (pas de log de l'INPUT), ou script ad-hoc non commit. À cadrer au moment du chantier T-202. Chantier d'origine : T-200.
- **T-230 [Technique] Cas CP outre-mer (DOM-TOM) : seuil de distance ou message dédié sur DistanceWidget** (issu rapport technique comité T-200 round 2, 03/05/2026) — un visiteur saisissant un CP outre-mer (97xxx, 98xxx) géocode légitimement, mais la distance Haversine vers un producteur métropolitain (3000-9000 km) devient un argument contre-productif (« plus loin que la grande distribution »). Prévoir un seuil au-delà duquel le widget bascule sur un message dédié, ou un disclaimer contextuel. Chantier d'origine : T-200.
- **T-231 [Technique] Documenter le compromis précision Haversine après arrondi à ~1 km** (issu rapport technique comité T-200 round 2, 03/05/2026) — pour un producteur situé à <10 km du visiteur, l'arrondi à 2 décimales peut produire un écart affiché de ±1-2 km par rapport à la distance réelle. Documenter ce compromis dans la doc dev (ou un commentaire central) et envisager un arrondi serveur plus fin (3 décimales = ~100 m) si l'étude T-227 conclut que la fuite est acceptable à cette granularité. Chantier d'origine : T-200. **r3 : modèle de menace + précision retenue désormais documentés explicitement dans `lib/producers/coords.ts`. La décision de fond (2 décimales vs grille commune) reste ouverte sous T-217.**

#### Follow-ups comité review T-200 (round 3, 03/05/2026)

> Rappel r3 : sur la branche, le round a déjà appliqué (a) enrichissement RGPD du widget distance (caractère facultatif + renvoi politique de conf, en plus de finalité/durée/sous-traitant/non-persistance déjà en place r2), (b) audit transverse coords producteur → trou comblé sur `app/(consumer)/compte/commandes/[id]` (les coords brutes y étaient exposées au consumer authentifié), (c) doc coords.ts enrichie (modèle de menace, déterminisme anti-trilatération, sites d'appel autorisés), (d) tests durcis (déterminisme stress 10k itérations, idempotence, contrat sécurité sur `/api/producers/search`), (e) smoke Playwright durci (assertion explicite sur le chiffre comparatif `~1500 km`, pas seulement le label). Les follow-ups ci-dessous sont les chantiers à part dégagés du rapport.

- **T-232 [Conformité] Mécanisme de rectification continue des 4 indicateurs producteur** (issu rapport conformité comité T-200 round 3, 03/05/2026) — droit de rectification RGPD : un producteur doit pouvoir corriger après coup `mode_elevage`, `alimentation`, `densite_animale` (et la lat/lng issue du géocodage adresse) sans dépendre du support. UI dédiée dans `/ma-page` producteur, à articuler avec T-203 (relance des producteurs déjà onboardés) et T-212 (aperçu temps réel). Enjeu de fiabilité dans le temps des allégations affichées. Chantier d'origine : T-200.
- **T-233 [Produit] Fallback wording « jusqu'à Ferme X » pour noms longs ou avec article** (issu rapport produit comité T-200 round 3, 03/05/2026) — l'amorce du DistanceWidget concatène brutalement le nom (« jusqu'à La Ferme des Quatre Vents »). Si le nom commence par un article (« La Ferme… ») le rendu est correct mais lourd ; si le nom est très long (>30 chars), le fallback typographique sur mobile peut casser l'équilibre du bloc. Prévoir une troncature ou une formulation alternative. Chantier d'origine : T-200.
- **T-234 [Produit] Audit transverse des messages d'erreur de validation client** (issu rapport produit comité T-200 round 3, 03/05/2026) — la regex CP a un message `"Code postal invalide (5 chiffres attendus)"` court et factuel. Vérifier que les autres formulaires consumer (téléphone, email, password, etc.) ont une homogénéité de ton, de longueur, et d'emplacement (sous le champ vs role=alert global). Pas spécifique T-200 mais point d'entrée légitime puisque T-200 ajoute le widget CP. Chantier d'origine : T-200.
- **T-235 [Sécurité] Exclusion lat/lng précise au niveau RLS / vue Supabase** (issu rapport sécurité comité T-200 round 3, 03/05/2026) — le floutage `roundCoord` est aujourd'hui appliqué au niveau applicatif (route handlers + Server Components). Une régression future (nouvelle route, nouveau service) qui oublierait le helper réintroduirait la fuite. Idéal : créer une `view producers_public` qui projette les coords arrondies au niveau Postgres, et basculer toutes les lectures consumer dessus. Defense in depth + une seule source de vérité. Chantier d'origine : T-200, en complément de T-217 et T-218.
- **T-236 [Sécurité] Rate-limit sur `/api/producers/search`** (issu rapport sécurité comité T-200 round 3, 03/05/2026) — sans limite, un attaquant peut énumérer la distance ferme→retrait pour un grand nombre de codes postaux et reconstruire la position approximative de la ferme malgré le flou (trilatération inverse côté client). À cadrer avec le besoin de pagination/scraping légitime. À traiter avant ouverture publique. Chantier d'origine : T-200.
- **T-237 [Technique] Test client interactif du DistanceWidget** (issu rapport technique comité T-200 round 3, 03/05/2026) — les 6 tests SSR sur `ScoreCarbonBlock` valident les cas de rendu mais pas le comportement interactif (saisie CP → activation bouton OK selon regex, gestion d'erreur géoloc denied/timeout, persistance sessionStorage entre visites). Ajouter une suite `@testing-library/react` + `userEvent` qui couvre ces interactions. Chantier d'origine : T-200.
- **T-238 [Technique] Scan automatique « no raw coords leak » sur toutes les routes API** (issu rapport technique comité T-200 round 3, 03/05/2026) — aujourd'hui le contrat anti-fuite est verrouillé par 1 test contractuel par route exposée (`/api/producers/search` en r3, à étendre manuellement à chaque nouvelle surface). À terme : un test de méta-niveau qui scanne `app/api/**` et `app/(public|consumer)/**` à la recherche de SELECT contenant `latitude/longitude` non suivis d'un `roundCoord` dans le même fichier, et fail au premier hit. Diminue le risque de régression silencieuse. Complément de T-235 (RLS-level mitigation). Chantier d'origine : T-200.
- **T-239** : T-200 follow-up — déplacer le bloc « Au plus près de l'éleveur » sous la section Produits sur la fiche producteur publique. Argument : le consumer veut voir ce que vend le producteur en premier, pas la fiche d'identité écolo. (Source : test utilisateur Romain)
- **T-240** : T-200 follow-up — compacter le widget distance. Trop volumineux dans son état actuel. Idée : version repliée par défaut "📍 Voir la distance" en 1 ligne, déploiement au clic. (Source : test utilisateur Romain)
- **T-241** [✓ DONE] : T-200 follow-up — Persister la déclaration sur l'honneur en base (timestamp + snapshot des 4 valeurs déclarées + version du wording certifié) pour traçabilité DGCCRF. Aujourd'hui c'est validé via Zod mais non persisté. Avant ouverture publique.
- **T-242** : T-200 follow-up — Mettre à jour les CGU producteur pour intégrer la responsabilité déclarative sur les 4 indicateurs (mode d'élevage, alimentation, densité, distance) et les conséquences en cas de fausse déclaration. Avant ouverture publique.


#### Follow-ups comité review T-200 (round 4, 03/05/2026)

> Rappel r4 : sur la branche, le round a appliqué une seule modif code (retrait du renvoi « voir notre politique de confidentialité » dans `DistanceWidget.tsx` tant que la page n'existe pas — note opérationnelle ajoutée à T-207 pour le go-live). Toutes les autres demandes du comité r4 étaient déjà couvertes par les rounds précédents : « à vol d'oiseau » en dur partout (lignes 174/232/288 de `DistanceWidget.tsx`), wording grand public des 10 libellés enum (`*_PUBLIC_LABELS` dans `score-carbone-enums.ts`), label comparatif GMS factuel non dénigrant (`GMS_DISTANCE_SOURCE_LABEL`), audit transverse coords (T-200 r3), validation CP côté serveur (lib `geocode-postal.ts` revérifie le format avant l'appel api-adresse). Les follow-ups ci-dessous sont les chantiers à part dégagés du rapport r4.

- **T-239 [Produit] QA mobile dédiée du bloc score carbone fiche producteur** (issu rapport produit comité T-200 round 4, 03/05/2026) — passage QA dédié sur écrans étroits (320 → 414 px) du bloc « Notre démarche » : rendu des 4 indicateurs en colonne unique vs grille, lisibilité du visuel comparatif km, taille des touch targets du DistanceWidget (bouton « Utiliser ma position » et OK CP doivent rester ≥ 44 px hauteur), troncature du nom de producteur dans l'amorce. À cadrer avec T-233 (fallback nom long) et T-215 (a11y pills). Chantier d'origine : T-200.
- **T-240 [Produit] État vide explicite quand un producteur n'a pas renseigné les 4 indicateurs** (issu rapport produit comité T-200 round 4, 03/05/2026) — aujourd'hui `ScoreCarbonBlock` early-return entièrement le bloc si aucune donnée n'est présente, ce qui « cache » la démarche aux consumers visitant un producteur récent ou non re-onboardé. À trancher : afficher un placeholder neutre (« Ce producteur n'a pas encore renseigné ses indicateurs ») ou maintenir le masquage. Décision produit en lien avec T-203 (relance des producteurs déjà onboardés). Chantier d'origine : T-200.
- **T-241 [Produit] Micro-tooltips d'aide au choix dans l'onboarding `StepInfos`** (issu rapport produit comité T-200 round 4, 03/05/2026) — les `*_HINTS` actuels sont rendus en-dessous des radios mais restent abstraits pour un producteur peu à l'aise avec la nomenclature. Ajouter un mini-bouton « ? » à côté de chaque option qui ouvre une explication concrète avec exemple (ex : « Plein air = poules dehors toute la journée, abri uniquement la nuit »). Impact direct sur la fiabilité de l'allégation côté consumer. Distinct de T-212 (aperçu temps réel du rendu fiche), complémentaire. Chantier d'origine : T-200.
- **T-242 [Conformité] Documenter publiquement la méthodologie de distance « à vol d'oiseau »** (issu rapport conformité comité T-200 round 4, 03/05/2026) — dans une page mentions légales / FAQ : préciser que la distance affichée est calculée par formule Haversine entre la position consumer (géoloc ou centroïde CP) et la position producteur (arrondie à 2 décimales = ~1 km), qu'elle ne tient pas compte du réseau routier, et qu'en cas de multi-drives le point retenu est celui de la fiche producteur. Utile en cas de contestation DGCCRF ou consumer sur un écart distance affichée vs distance routière. Chantier d'origine : T-200.
- **T-243 [Conformité] Versioning + historisation des valeurs d'enums score carbone** (issu rapport conformité comité T-200 round 4, 03/05/2026) — si la liste des valeurs `MODE_ELEVAGE_VALUES` / `ALIMENTATION_VALUES` / `DENSITE_ANIMALE_VALUES` évolue (ajout, renommage, suppression), les producteurs déjà classés doivent être informés et la fiche historisée pour audit (allégation t-1 différente d'allégation t-2). À cadrer : table d'historique par producer × indicateur, ou snapshot lors de chaque modif. Prérequis pour audit DGCCRF rétrospectif. Chantier d'origine : T-200.
- **T-244 [Business] Priorisation explicite T-232 → T-238 et T-239 → T-243 vs jalon « passage en Live »** (issu rapport business comité T-200 round 4, 03/05/2026) — le rapport r4 souligne que certains follow-ups sont des **prérequis Live** (T-235 vue Supabase floutée, T-236 rate-limit search, T-238 scan auto anti-fuite, T-207 politique de conf à publier, T-202 backfill lat/lng) et d'autres de la dette long terme. Construire la liste « bloquants Live » consolidée à partir des 12 follow-ups T-200 r3+r4 et l'attacher à la checklist de passage en Live (chantier go-live à venir). Chantier d'origine : T-200.

#### Follow-ups comité review T-239+T-240 (round 1, 04/05/2026)

> Rappel r1 : sur la branche, le round a appliqué (a) feedback de chargement explicite sur le bouton OK CP (label « Calcul… » + input disabled pendant la requête, aligne sur le pattern géoloc), (b) tests Vitest unitaires des 5 chemins de rendu du DistanceWidget (replié vide, replié avec session, déployé sans session, déployé avec session, producer sans coords) — verrou anti-régression de la refonte disclosure et de la mention RGPD r4, (c) clear sessionStorage en init script Playwright pour garantir que le smoke démarre toujours sur l'état « Voir la distance jusqu'à toi ». Diagnostic préalable a confirmé que SSR hydratation et scoping sessionStorage étaient déjà gérés correctement avant ce round (useEffect, coords-only sans slug). Décisions explicitées : tap target « Masquer » accepté tel quel (action accessoire, info principale visible), tutoiement maintenu (TerrOir tutoie partout par convention de produit). Les follow-ups ci-dessous sont les chantiers à part dégagés du rapport.

- **T-245 [Business] Instrumentation scroll-depth + clic produit fiche producteur** (issu rapport business comité T-239+T-240 round 1, 04/05/2026) — le déplacement du bloc « Démarche » sous les avis a été décidé sur un test utilisateur unique (Romain) sans télémétrie. Avant ouverture publique, ajouter un événement scroll-depth (25/50/75/100 %) et un événement « clic produit » sur la fiche producteur, pour valider en prod que le nouveau placement améliore réellement le passage Histoire → Produits. Complément de T-201 (mesure d'usage globale du widget distance). Chantier d'origine : T-239+T-240.
- **T-246 [Business] Instrumentation taux d'expansion du widget distance replié** (issu rapport business comité T-239+T-240 round 1, 04/05/2026) — savoir quel pourcentage de consumers cliquent « Voir la distance jusqu'à toi » permet de doser plus tard la visibilité du bloc Démarche / distance, surtout au moment du scaling Sarthe → Pays de la Loire (où la distance devient un argument plus discriminant). Chantier d'origine : T-239+T-240, complément T-201.
- **T-247 [Business] Persistance opt-in de la position consumer (au-delà du sessionStorage)** (issu rapport business comité T-239+T-240 round 1, 04/05/2026) — aujourd'hui `terroir_geo_session` est purgé à la fermeture d'onglet (par construction). Si TerrOir veut plus tard proposer un tri « producteurs proches de chez moi » ou des notifications de proximité, il faudra une persistance opt-in (cookie ou champ profil consumer). À garder en tête, pas urgent. Articulation avec T-213 (pré-remplissage global de la position consumer header / compte). Chantier d'origine : T-239+T-240.
- **T-248 [Conformité] Cohérence registre des traitements ↔ mention in-situ widget distance** (issu rapport conformité comité T-239+T-240 round 1, 04/05/2026) — avant Live, vérifier que la Politique de confidentialité globale du site (T-041, T-207) mentionne explicitement le traitement « calcul de distance producteur ↔ consommateur » avec sous-traitant `api-adresse.data.gouv.fr`, en cohérence avec la mention RGPD déjà affichée dans le widget. Évite la divergence registre des traitements ↔ information utilisateur. Chantier d'origine : T-239+T-240.
- **T-249 [Conformité / Sécurité] Audit logs serveur — confirmer non-capture des CP/coords transmis au calcul Haversine** (issu rapport conformité comité T-239+T-240 round 1, 04/05/2026) — vérifier sur l'infra (Vercel function logs, Resend, sentry futur, etc.) qu'aucun log serveur ne capture le code postal ni les coordonnées arrondies. Le calcul Haversine est purement client side mais une régression future qui passerait la position au serveur ne doit jamais être loggée. Audit pre-Live. Chantier d'origine : T-239+T-240.
- **T-250 [Produit] Audit global tutoiement vs vouvoiement parcours consumer** (issu rapport produit comité T-239+T-240 round 1, 04/05/2026) — Romain a confirmé que TerrOir tutoie partout par convention de produit, mais aucun audit transverse n'a été fait. Avant Live, balayer l'ensemble des écrans consumer (home, fiche produit, panier, checkout, compte, mails Resend) pour figer le ton et corriger les éventuelles incohérences. Chantier d'origine : T-239+T-240.
- **T-251 [Produit] Repenser la section « Démarche / score-carbone » maintenant qu'elle est en bas de page** (issu rapport produit comité T-239+T-240 round 1, 04/05/2026) — déplacer le bloc sous Avis a un effet de bord prévisible : moins de consumers le verront. Travailler titre / accroche / incitation à scroller (et éventuellement un teaser « rappel terroir » plus haut dans la page) pour que la démarche reste lue. À cadrer avec T-211 (déclinaison par métier producteur) et T-245 (mesure scroll-depth). Chantier d'origine : T-239+T-240.
- **T-252 [Produit] Test utilisateur ciblé seniors sur le widget distance replié** (issu rapport produit comité T-239+T-240 round 1, 04/05/2026) — le pattern « replié → cliquer pour déplier » est intuitif pour un consumer mobile averti, à valider auprès de profils seniors qui pourraient le percevoir comme « un bouton qui ne fait rien d'utile ». Chantier d'origine : T-239+T-240, articulation T-239 r4 (QA mobile).
- **T-253 [Sécurité] Audit pre-Live `sessionStorage` DistanceWidget — non-fuite tiers** (issu rapport sécurité comité T-239+T-240 round 1, 04/05/2026) — confirmer que la donnée stockée dans `terroir_geo_session` (lat/lng + source) ne fuite vers aucun tiers via les requêtes réseau (Resend, Stripe, analytics éventuels Vercel/Plausible/etc.) et que rien n'est persisté au-delà de la session navigateur. Documenter dans la politique de confidentialité au même titre que la mention déjà affichée dans le widget. Articulation avec T-207, T-208, T-248. Chantier d'origine : T-239+T-240.
- **T-254 [Sécurité] Audit champs sensibles renvoyés par la fiche producteur publique** (issu rapport sécurité comité T-239+T-240 round 1, 04/05/2026) — vérifier (audit séparé, hors périmètre T-239/T-240) que `/producteurs/[slug]` ne renvoie pas, dans les données injectées côté client pour les blocs réordonnés, des champs sensibles du producteur (email perso, téléphone, adresse précise non floutée). Check de routine à refaire après chaque réorg de page. Articulation avec T-217 (politique uniforme de floutage), T-218 (audit RLS table `producers`), T-235 (vue Supabase floutée), T-238 (scan auto anti-fuite). Chantier d'origine : T-239+T-240.
- **T-255 [Technique] Lint anti-apostrophe courbe (U+2019) dans les sources TS/TSX** (issu rapport technique comité T-239+T-240 round 1, 04/05/2026) — pendant le chantier, une apostrophe courbe `’` U+2019 introduite dans un label JSX a fait diverger le smoke Playwright (qui matchait l'apostrophe droite ASCII). Mettre en place une règle ESLint custom (ou plugin existant) qui bloque U+2019 dans les sources `.ts`/`.tsx` pour empêcher la récidive. Le rendu visuel courbe peut toujours s'obtenir via `&rsquo;`/`&apos;` selon le contexte. Chantier d'origine : T-239+T-240.
- **T-256 [Technique / dette a11y consciente] Pattern disclosure du design system : standardiser sur toggle `aria-expanded`** (issu rapport technique comité T-239+T-240 round 1, 04/05/2026 ; statut « dette a11y consciente » confirmé round 2, 04/05/2026) — l'implémentation actuelle du DistanceWidget utilise un bouton compact pour ouvrir + un lien « Masquer » (12 px sans padding) pour fermer. C'est un pattern non-standard : un vrai toggle `aria-expanded` (un seul élément interactif qui ouvre et ferme, et tap target ≥ 44 px sur les deux états) serait plus cohérent avec les conventions a11y. **Décision Romain (round 1) : on accepte la dette a11y de la version actuelle puisque l'info principale reste visible et le « Masquer » est une action accessoire** — à rembourser au moment du refacto disclosure transverse, pas avant. À cadrer si d'autres composants disclosure apparaissent dans le projet (FAQ, sections optionnelles, etc.). Chantier d'origine : T-239+T-240.

#### Follow-ups comité review T-239+T-240 (round 2, 04/05/2026)

> Rappel r2 : sur la branche, le round a appliqué (a) renforcement des assertions métier des 5 tests Vitest DistanceWidget (attribut `disabled` du bouton OK avant validation CP, plage de distance plausible 150–250 km pour Paris↔Sarthe, présence d'un width inline sur la barre comparative, valeur `aria-busy` du bouton OK, absence du formulaire CP en mode résultat), (b) test E2E qui vérifie l'ordre `Histoire → Produits → Avis → Démarche` via `getBoundingClientRect().top` — verrou contre une régression silencieuse de placement, (c) reformulation du commentaire `IS_REACT_ACT_ENVIRONMENT` qui décrivait à tort le flag comme « silencieur de warning » (le flag les active, c'est le wrap correct dans `act()` qui les évite). Diagnostic préalable a confirmé : SSR hydratation toujours OK (lecture sessionStorage dans useEffect), deps `useMemo` Haversine OK (`session, producerLat, producerLng`), `addInitScript` Playwright posé avant tout `goto` et déjà entouré try/catch, mention RGPD accessible en 1 clic depuis l'état compact (deux chemins : `DistanceWidget.tsx:248` sans session, `:391` avec session). Décisions explicitées : tutoiement maintenu (faux point comité), verdict Business MODIFS r2 ignoré (que des TODO, pas de modif identifiable). Les follow-ups ci-dessous sont les chantiers à part dégagés du rapport r2.
>
> **Pré-Live RGPD critique** : T-261, T-262, T-263, T-264, T-265 sont à intégrer à la checklist de passage en Live (référencer ces IDs depuis T-244).

- **T-257 [Business / pré-Live] Prioriser explicitement T-245 + T-246 en P1 avant ouverture publique** (issu rapport business comité T-239+T-240 round 2, 04/05/2026) — sans baseline mesurée du scroll-depth produits (T-245) et du taux d'expansion du widget distance (T-246), le bénéfice de T-239+T-240 reste invérifiable. Le comité r2 demande de **figer T-245 et T-246 en bloquants Live** (et non « plus tard indéfini »). Articulation T-244 (consolidation des bloquants Live). Chantier d'origine : T-239+T-240.
- **T-258 [Business] Définir le KPI cible du widget distance déplié** (issu rapport business comité T-239+T-240 round 2, 04/05/2026) — quel pourcentage minimal de consumers cliquant « Voir la distance jusqu'à toi » justifie de garder la fonctionnalité ? Sous ce seuil → la complexité du composant ne se justifie pas, on simplifie ou on retire. À arbitrer post-Live, dépend des chiffres mesurés via T-246. Chantier d'origine : T-239+T-240.
- **T-259 [Business] Vérifier que le bloc « Démarche » descendu en bas reste lu** (issu rapport business comité T-239+T-240 round 2, 04/05/2026) — règle d'arbitrage : si moins de 20 % des consumers atteignent le bloc Démarche d'après le scroll-depth (T-245), l'argument circuit court / bien-être animal perd son rôle de réassurance. Trois options à évaluer : (a) repositionnement intermédiaire, (b) teaser plus haut dans la fiche, (c) abandon. Articulation T-245, T-251. Chantier d'origine : T-239+T-240.
- **T-260 [Business] Anticiper l'impact de l'élargissement géo sur la pertinence du widget distance** (issu rapport business comité T-239+T-240 round 2, 04/05/2026) — la pertinence du widget change radicalement à l'échelle nationale. Un consumer parisien à 200 km d'un producteur sarthois ne fait pas le même arbitrage qu'un consumer manceau à 12 km. À ré-évaluer au moment du basculement Sarthe → Pays de la Loire → France (changement de palette de seuils, libellés, comparaison circuit long potentiellement à reformuler). Articulation T-247 (persistance position consumer), T-258 (KPI). Chantier d'origine : T-239+T-240.
- **T-261 [Conformité / pré-Live] Intégrer T-248 + T-249 + T-263 + T-264 + T-265 à la checklist RGPD pré-Live** (issu rapport conformité comité T-239+T-240 round 2, 04/05/2026) — les follow-ups conformité du widget distance (registre des traitements ↔ politique de conf, audit logs, wording in-situ, CSP, exclusion CP des trackers front) doivent être consolidés dans la checklist de passage en Live, pas traités en ordre dispersé. Articulation T-244, T-207. Chantier d'origine : T-239+T-240.
- **T-262 [Conformité] CGU/CGV pré-Live : mention « distance à vol d'oiseau » et usage de `sessionStorage` côté consumer** (issu rapport conformité comité T-239+T-240 round 2, 04/05/2026) — au moment de la rédaction des CGU/CGV pré-Live, s'assurer qu'elles couvrent explicitement (i) la fonctionnalité de calcul de distance, (ii) l'usage de `sessionStorage` côté consumer même non-persistant (durée = onglet ouvert), (iii) la finalité associée (calcul de proximité avec le producteur). Articulation T-248 (cohérence registre des traitements). Chantier d'origine : T-239+T-240.
- **T-263 [Sécurité] Wording mention RGPD du widget : ajouter explicitement « stockage navigateur, durée = onglet ouvert »** (issu rapport sécurité comité T-239+T-240 round 2, 04/05/2026) — la `PrivacyNote` actuelle dit « reste dans ton navigateur (session uniquement, jamais envoyée ni enregistrée sur nos serveurs) ». Le comité r2 demande d'ajouter le mot « **`sessionStorage`** » (ou une formulation équivalente technique) pour que la durée soit techniquement opposable, en cohérence avec ce qui sera écrit dans la politique de conf globale (T-248). À recouper avec T-248 avant Live. Chantier d'origine : T-239+T-240.
- **T-264 [Sécurité / pré-Live] CSP durcie `connect-src` / `script-src` pour limiter l'exfiltration de `sessionStorage`** (issu rapport sécurité comité T-239+T-240 round 2, 04/05/2026) — un XSS futur pourrait exfiltrer le contenu de `terroir_geo_session` (lat/lng arrondies). Une CSP stricte avec `connect-src` whitelistant uniquement Supabase + `api-adresse.data.gouv.fr` + Stripe + Resend, et `script-src` sans `unsafe-inline`, réduit drastiquement la surface d'exfil. Complément de T-253. Chantier d'origine : T-239+T-240.
- **T-265 [Sécurité] Étendre T-249 côté client : exclure le code postal des trackers front éventuels** (issu rapport sécurité comité T-239+T-240 round 2, 04/05/2026) — T-249 cible les logs serveur. Le comité r2 demande d'étendre côté client : si Plausible / PostHog / GA / autre tracker est ajouté un jour, vérifier (a) que les events ne capturent jamais le CP saisi (pas de `track('cp_input', { cp: '75001' })`), (b) que les `referrer` / URL ne contiennent pas le CP en query string. Chantier d'origine : T-239+T-240.
- **T-266 [Technique] Étendre T-255 : lint sur les clés `sessionStorage`/`localStorage` non préfixées `terroir_`** (issu rapport technique comité T-239+T-240 round 2, 04/05/2026) — au-delà de l'apostrophe courbe (T-255), une règle lint custom devrait bloquer toute utilisation de `sessionStorage.setItem('xxx', …)` ou `localStorage.setItem('xxx', …)` où la clé `xxx` ne commence pas par `terroir_` (préfixe de namespace, anti-collision avec extensions navigateur ou bibliothèques tierces). Chantier d'origine : T-239+T-240.
- **T-267 [Technique] Documenter dans `DistanceWidget.tsx` le choix « clé `terroir_geo_session` globale partagée entre producteurs »** (issu rapport technique comité T-239+T-240 round 2, 04/05/2026) — le diagnostic r2 a confirmé que la clé est volontairement globale (on stocke des coords consumer, pas une distance pré-calculée, donc Haversine est recalculé par producteur via `useMemo`). Mais le prochain dev qui lira `terroir_geo_session` sans contexte risque d'introduire une régression en y stockant la distance, ce qui ferait apparaître « 12 km » sur une autre fiche producteur. Ajouter un commentaire `WHY` au-dessus de `SESSION_KEY` dans `DistanceWidget.tsx:16` pour figer cette invariant. Chantier d'origine : T-239+T-240.
- **T-268 [Technique] Introduire `@testing-library/react` au moment du refacto disclosure (T-256)** (issu rapport technique comité T-239+T-240 round 2, 04/05/2026) — les tests Vitest actuels du DistanceWidget utilisent `react-dom/client` + `act()` brut pour limiter la surface de deps (1 dep ajoutée seulement, jsdom). Ce pattern n'est pas tenable au-delà de ce chantier dès qu'on doit tester des interactions plus riches (saisie clavier, focus, query par rôle). Au moment de T-256 (refacto disclosure transverse), basculer sur `@testing-library/react` + `userEvent`. Articulation T-237. Chantier d'origine : T-239+T-240.
- **T-269 [Technique] Évaluer coût/bénéfice de l'env Vitest jsdom global vs ciblé par fichier** (issu rapport technique comité T-239+T-240 round 2, 04/05/2026) — actuellement seuls les fichiers de tests qui en ont besoin déclarent `// @vitest-environment jsdom` en pragma. Sur 1584 tests, basculer global aurait un coût (chaque test charge un DOM même quand il teste une lib pure). À benchmarker quand le nombre de tests UI augmentera (post-T-256). Décision : statu quo (pragma par fichier) tant que < 10 fichiers de tests UI. Chantier d'origine : T-239+T-240.

#### Follow-ups comité review T-239+T-240 (round 3, 04/05/2026)

> Rappel r3 : sur la branche, le round a appliqué (a) commentaire au-dessus du `useEffect` de lecture sessionStorage qui documente le choix produit (décision Romain r1) — session présente → bouton compact reste replié avec distance affichée, pas d'auto-expand ; (b) durcissement de `readSession` dans `DistanceWidget.tsx:32-57` : ajout `Number.isFinite` (rejette NaN qui passe `typeof === "number"`) et vérification des plages WGS84 (lat ∈ [-90, 90], lng ∈ [-180, 180]) — fallback silencieux sur l'état compact si données corrompues ; (c) test Vitest dédié couvrant 7 scénarios de sessionStorage corrompu (JSON invalide, champs manquants, mauvais typage, NaN, hors plage WGS84, source inconnue) — verrou anti-incident "donnée tierce malformée écrase notre clé". Diagnostic préalable a confirmé : wording bouton compact + tutoiement OK (`DistanceWidget.tsx:168-169`), tap target 44px OK (`DistanceWidget.tsx:269` `h-11`), feedback de chargement géoloc + CP OK, messages d'erreur OK, mention RGPD accessible en 1 clic depuis l'état compact OK, tests Vitest couvrent déjà explicitement les 2 états initiaux (a sans session, b avec session). Décisions explicitées : points produit déjà confirmés rounds 1 et 2 maintenus tels quels. Les follow-ups ci-dessous sont les chantiers à part dégagés du rapport r3.

- **T-270 [Business] Préciser dans T-258 le seuil minimal d'usage du widget distance** (issu rapport business comité T-239+T-240 round 3, 04/05/2026) — T-258 fixe le principe (« définir un KPI cible »), le comité r3 demande de préciser explicitement le seuil sous lequel on considère le widget mort-né et on envisage sa suppression. Sinon on porte indéfiniment du code RGPD-sensible pour rien (lecture sessionStorage défensive, mention RGPD, validation CP, audit pré-Live T-253 / T-263 / T-264 / T-265). À arbitrer post-Live, dépend des chiffres de T-246. Chantier d'origine : T-239+T-240.
- **T-271 [Business] Conditionner le bloc score-carbone à une distance seuil lors de l'élargissement géo** (issu rapport business comité T-239+T-240 round 3, 04/05/2026) — la promesse « au plus près de chez toi » perd son sens à 250 km (consumer parisien commandant chez un producteur sarthois). Au moment du basculement Sarthe → Pays de la Loire → France, à recroiser avec T-260 : (a) garder le bloc en bas inconditionnellement (faible visibilité, OK), ou (b) conditionner son affichage à une distance seuil. Articulation T-260, T-259 (mesure scroll-depth qui informera la décision). Chantier d'origine : T-239+T-240.
- **T-272 [Conformité] Valider explicitement dans l'audit RGPD transverse le pattern « clic pour déployer la mention RGPD »** (issu rapport conformité comité T-239+T-240 round 3, 04/05/2026) — la mention RGPD du widget n'est visible qu'après un clic sur le bouton compact. Le comité r3 considère cela acceptable (aucune donnée n'est collectée tant que l'utilisateur ne saisit rien : la mention est rendue avant la collecte, pas après) mais demande de le valider explicitement dans l'audit RGPD pré-Live, au même titre que les autres points T-261. Articulation T-261, T-244. Chantier d'origine : T-239+T-240.
- **T-273 [Produit / a11y] Audit a11y du widget en mode disclosure** (issu rapport produit comité T-239+T-240 round 3, 04/05/2026) — vérifier `aria-expanded`, `aria-controls`, focus management au déploiement (le focus doit-il aller sur le bouton géoloc ? le champ CP ? rester sur le bouton compact ?), comportement lecteur d'écran (NVDA, VoiceOver). La dette T-256 identifie déjà le pattern non-standard (toggle séparé du lien « Masquer ») ; T-273 cible spécifiquement l'audit a11y exécuté avant ouverture publique, pas le refacto. À prioriser dans la checklist pré-Live. Chantier d'origine : T-239+T-240.
- **T-274 [Sécurité] Vérification pre-Live : `sessionStorage` non-embarqué par outils de session-replay / monitoring front** (issu rapport sécurité comité T-239+T-240 round 3, 04/05/2026) — au-delà de T-265 (trackers analytics), si TerrOir intègre un jour Sentry/Datadog/LogRocket/FullStory, vérifier que le contenu de `terroir_geo_session` (lat/lng arrondies, source) n'est pas embarqué dans les replays/sessions/breadcrumbs envoyés au tiers. Audit à intégrer à la checklist pré-Live au même titre que T-261, T-264, T-265. Chantier d'origine : T-239+T-240.
- **T-275 [Sécurité] Garde-fou : aucun endpoint d'autocomplétion CP futur ne doit logger ni passer le CP en query string** (issu rapport sécurité comité T-239+T-240 round 3, 04/05/2026) — aujourd'hui le calcul de distance est purement client (appel direct browser → `api-adresse.data.gouv.fr`). Si une extension future ajoutait un endpoint d'autocomplétion CP côté serveur (suggestions saisie, rate-limit, cache), s'assurer que (a) aucun log serveur n'enregistre le CP saisi (cohérent avec T-249), (b) le CP n'est jamais transmis en query string GET (qui se retrouverait en clair dans les logs Vercel, le `Referer`, l'historique navigateur). Imposer POST + body. Chantier d'origine : T-239+T-240.
- **T-276 [Sécurité / pré-Live] Vérifier le scoping origine de `terroir_geo_session` à `terroir-local.fr`** (issu rapport sécurité comité T-239+T-240 round 3, 04/05/2026) — `sessionStorage` est scopé par origine (schema + host + port) par construction navigateur. Si TerrOir déploie un jour des sous-domaines applicatifs (`app.terroir-local.fr`, `producer.terroir-local.fr`, `consumer.terroir-local.fr`), chaque sous-domaine aurait son propre `sessionStorage` isolé — ce qui est OK pour notre cas (on ne veut pas que les sous-domaines se partagent les coords consumer) mais à valider explicitement à la pré-Live. Add à la checklist T-261. Chantier d'origine : T-239+T-240.
- **T-277 [Technique] Dériver ou documenter dans le test Vitest la plage `150-250 km` (Paris↔Sarthe)** (issu rapport technique comité T-239+T-240 round 3, 04/05/2026) — l'assertion `distance-widget.test.tsx:116-117` (`km > 150 && km < 250`) est une plage hardcodée plausible pour Paris↔Sarthe. Si la fixture producteur (coords) ou consumer (coords Paris) change un jour, l'assertion peut casser muettement (test toujours vert sur une plage absurde) ou exploser (test rouge sans diagnostic immédiat). Deux options : (a) extraire les coords en constantes documentées + commentaire avec calcul Haversine attendu, (b) calculer dynamiquement la plage attendue dans le test à partir des coords fixées. La version actuelle documente déjà via commentaire (lignes 102-110) — option (b) reste à évaluer si on étend la couverture interactive. Chantier d'origine : T-239+T-240.

#### Follow-ups comité review T-241 (round 2, 04/05/2026)

> Rappel r2 : sur la branche, le round a appliqué (a) bascule de la décision de re-persistance des `declaration_indicateurs_*` vers une RPC SQL `update_producer_onboarding` atomique (SELECT FOR UPDATE + CASE WHEN dans une seule transaction PostgreSQL — fin de la fenêtre lecture-modification non atomique du round 1) ; (b) ajout dans `lib/producers/declaration-veracite.ts` d'une map `DECLARATION_VERACITE_WORDINGS` archivant le TEXTE EXACT de chaque version du libellé certifié, pas juste le numéro — sans cela la trace probatoire DGCCRF perd son sens dès le bump v1.0 → v1.1 ; (c) sémantique « tous enums NULL » figée explicitement (préservation des colonnes historiques) + couverte par test ; (d) helper testable `shouldPersistDeclarationVeracite` miroir lisible de la logique CASE WHEN SQL, avec 7 cas unitaires couvrant création / édition changeante / édition inerte / tous-enums-NULL / case non cochée / changement partiel. Tests T-241 dans `complete-onboarding.test.ts` adaptés au contrat RPC.

- **T-278 [Business] Prévoir le passage v1.1 du wording de la déclaration sur l'honneur** (issu rapport business comité T-241 round 2, 04/05/2026) — quand le libellé évoluera (ex: clarification juridique, ajout d'un indicateur), définir le moment où on force une re-coche des producteurs déjà certifiés en v1.0 (notification email, blocage soft du dashboard producteur, bandeau persistant). Sans ce mécanisme, la map `DECLARATION_VERACITE_WORDINGS` continuera de croître sans que les producteurs anciens ne reconfirment, et la version courante deviendra un artefact mort. Articulation T-282 (gouvernance wording) et T-283 (UX re-coche). Chantier d'origine : T-241.
- **T-279 [Business] Documenter dans le runbook admin l'extraction du snapshot DGCCRF** (issu rapport business comité T-241 round 2, 04/05/2026) — préciser comment extraire `declaration_indicateurs_veracite_at`, `declaration_indicateurs_snapshot` et `declaration_indicateurs_wording_version` en cas de réquisition DGCCRF (export CSV via UI admin futur ? requête SQL type prête à exécuter ? format de remise au contrôleur ?). Aujourd'hui ces 3 colonnes sont peuplées en base mais leur extraction reste connaissance tribale chez Romain. À documenter dans `docs/METHODOLOGY.md` ou un runbook admin dédié avant ouverture publique. Chantier d'origine : T-241.
- **T-280 [Business] Étendre le pattern snapshot daté + version aux autres déclarations engageantes producteur** (issu rapport business comité T-241 round 2, 04/05/2026) — quand de nouvelles déclarations engageantes producteur arriveront (origine produits, étiquetage allergènes évoqué dans le contexte alimentaire, certifications labels…), capitaliser sur le pattern centralisé créé par T-241 : map `_WORDINGS` versionnée + RPC atomique de re-persistance. Pas de re-développement ad hoc à chaque nouvelle case sur l'honneur. Chantier d'origine : T-241.
- **T-281 [Business] Au passage en Live : vérifier que les producteurs existants sont re-soumis au flow d'onboarding ou re-certifiés** (issu rapport business comité T-241 round 2, 04/05/2026) — les producteurs créés avant T-241 ont les 3 colonnes `declaration_indicateurs_*` à NULL (pas de backfill — la migration T-241 a posé la nouvelle infra mais ne rétroactive pas les anciens enregistrements). Avant ouverture publique consumer, s'assurer qu'aucun producteur visible n'est dans cet état (forcer re-coche, ou les masquer tant que NULL, ou bloquer le passage Live tant que tous certifiés). À cadrer dans la checklist pré-Live. Chantier d'origine : T-241.
- **T-282 [Business / Conformité] Procédure de gouvernance du wording certifié** (issu rapport conformité comité T-241 round 2, 04/05/2026) — définir explicitement : qui valide le passage v1.0 → v1.1 (Romain seul ? juriste obligatoire ?), où est archivé l'historique des libellés (la map `DECLARATION_VERACITE_WORDINGS` joue ce rôle, mais à formaliser dans une procédure écrite), comment relier un `wording_version` en base au texte exact qui était affiché à ce moment-là (la map fait ce lien — à documenter comme single source of truth). Sans procédure écrite, le snapshot perd sa valeur probatoire dès qu'on touche au wording. Chantier d'origine : T-241.
- **T-283 [Business / Mesure] Suivi taux de producteurs avec déclaration persistée vs NULL** (issu rapport business comité T-241 round 2, 04/05/2026) — ajouter une métrique (admin dashboard ou requête SQL type) du ratio producteurs avec `declaration_indicateurs_veracite_at IS NOT NULL` vs total producteurs actifs, pour piloter la qualité du parc producteurs au fil de l'élargissement Sarthe → Pays de la Loire → France. Articulation T-281 (la métrique informera la décision d'ouverture pré-Live). Chantier d'origine : T-241.
- **T-284 [Conformité] Documenter dans le registre RGPD (Article 30) la finalité et la durée de conservation des 3 colonnes `declaration_indicateurs_*`** (issu rapport conformité comité T-241 round 2, 04/05/2026) — données déclaratives à valeur probatoire DGCCRF, à conserver au minimum le temps de la prescription DGCCRF applicable (typiquement 2 ans après la fin de la relation commerciale, à confirmer avec le juriste). Finalité : preuve de l'engagement déclaratif du producteur sur les indicateurs score-carbone publics. Base légale : intérêt légitime (loyauté de l'information consumer + obligation légale DGCCRF). À ajouter au registre des traitements avant Live. Chantier d'origine : T-241.
- **T-285 [Conformité] Politique de purge/anonymisation des `declaration_indicateurs_*` lors de la suppression d'un compte producteur** (issu rapport conformité comité T-241 round 2, 04/05/2026) — arbitrage à trancher entre droit à l'effacement RGPD et obligation de conservation probatoire DGCCRF : (a) conservation en archive intermédiaire (table `archived_declarations`) pendant le délai DGCCRF puis purge, ou (b) anonymisation des colonnes après expiration du délai (suppression du lien user_id → snapshot mais conservation du snapshot pour stats agrégées). À spécifier avec le juriste, à intégrer à la mécanique RGPD existante (`delete_user_account` RPC). Articulation T-284, T-289 (rétention sécurité). Chantier d'origine : T-241.
- **T-286 [Conformité] Ajouter dans les CGU producteur (ou la charte producteur) une clause explicite sur l'horodatage de la déclaration** (issu rapport conformité comité T-241 round 2, 04/05/2026) — information loyale RGPD : le producteur coche aujourd'hui sans être informé que son geste est tracé en base (timestamp, snapshot des valeurs, version du libellé). Clause à ajouter : « la coche de la déclaration sur l'honneur est horodatée et conservée à des fins probatoires, et toute modification d'un indicateur entraîne une nouvelle déclaration datée ». Articulation T-041 (pages légales) + T-282 (gouvernance wording). Chantier d'origine : T-241.
- **T-287 [Conformité / Sécurité] Vérifier les RLS Supabase sur les 3 colonnes `declaration_indicateurs_*` avant Live** (issu rapports conformité+sécurité comité T-241 round 2, 04/05/2026) — lecture admin uniquement (pas exposable à un autre producteur ni à un consumer dans `producers_public_view`), et écriture interdite par self-update producteur direct (un producteur ne doit pas pouvoir forger lui-même son `_veracite_at` / `_snapshot` / `_wording_version` via PostgREST ou client Supabase ; le seul write path autorisé est la RPC `update_producer_onboarding` exécutée en service_role). Si la RLS UPDATE laisse passer ces colonnes en self-update, la valeur probante de la trace DGCCRF s'effondre. À intégrer à l'audit RLS pré-Live (T-003). Articulation T-003. Chantier d'origine : T-241.
- **T-288 [Produit] Chantier futur « wording v1.1 » de la case déclaration sur l'honneur** (issu rapport produit comité T-241 round 2, 04/05/2026) — quand le libellé évoluera, définir le parcours UX pour les producteurs déjà engagés en v1.0 : re-coche explicite avec mise en avant de la nouveauté ? bandeau d'information sans blocage ? rien ? — pour rester cohérent avec la logique de versioning introduite par T-241. Articulation T-278 (déclencheur business) + T-282 (gouvernance). Chantier d'origine : T-241.
- **T-289 [Produit] UX explicite de re-coche après changement d'indicateur sur la page d'édition producteur future** (issu rapport produit comité T-241 round 2, 04/05/2026) — aujourd'hui, si un producteur édite son fiche et change un enum, la RPC re-date silencieusement la déclaration (re-coche datée). Lors d'un prochain passage UX sur l'écran d'édition producteur, vérifier que ce comportement soit explicite côté utilisateur (message du type « En modifiant cet indicateur, vous re-confirmez votre déclaration sur l'honneur datée d'aujourd'hui ») pour éviter qu'un producteur ait l'impression de re-signer sans le savoir. Articulation T-292 (extraction fonction métier au moment de la page d'édition). Chantier d'origine : T-241.
- **T-290 [Sécurité / pré-Live] Politique de rétention des traces `declaration_indicateurs_*` cohérente avec la durée RGPD côté producteur** (issu rapport sécurité comité T-241 round 2, 04/05/2026) — croisement sécurité × conformité : la rétention de ces traces probatoires doit s'aligner avec la durée RGPD côté producteur définie en T-284/T-285. Aujourd'hui aucune politique formalisée → décider conjointement (juriste + sécu) avant Live. Articulation T-284, T-285. Chantier d'origine : T-241.
- **T-291 [Technique] Application de la migration SQL T-241 via MCP Supabase post-merge** (issu rapport technique comité T-241 round 2, 04/05/2026) — workflow `docs/METHODOLOGY.md` §Migrations DB : après merge sur master, appliquer `20260504100000_t241_declaration_veracite_persistance.sql` via MCP Supabase pour poser les 3 colonnes ET la fonction RPC `update_producer_onboarding`. À tracker pour ne pas merger le code applicatif (server action qui appelle la RPC) avant que la migration soit appliquée côté prod — sinon les UPDATE casseront en runtime avec « function update_producer_onboarding does not exist ». Chantier d'origine : T-241.
- **T-292 [Technique] Contrainte CHECK sur `declaration_indicateurs_wording_version`** (issu rapport technique comité T-241 round 2, 04/05/2026) — ajouter une contrainte CHECK sur les valeurs admises (`'v1.0'`, `'v1.1'`, …) côté DB pour éviter les fautes de frappe le jour du bump. Pas bloquant aujourd'hui (la valeur est posée par la constante `DECLARATION_VERACITE_WORDING_VERSION` côté code), mais ceinture+bretelles utile au moment où plusieurs versions cohabiteront. Migration légère (ALTER TABLE ADD CONSTRAINT). Chantier d'origine : T-241.
- **T-293 [Technique] Runbook bump wording v1.1** (issu rapport technique comité T-241 round 2, 04/05/2026) — quand le wording évoluera, suivre le runbook : (a) ✅ FAIT par BL-2 (PR #108) — entrée `DECLARATION_VERACITE_WORDINGS["v1.1"]` archivée dans `lib/producers/declaration-veracite.ts` (NE JAMAIS toucher v1.0), libellé placeholder à raffiner avec juriste avant bump effectif (cf. T-282) ; (b) bumper `DECLARATION_VERACITE_WORDING_VERSION = "v1.1"` dans `lib/producers/declaration-veracite.ts` ; (c) **NO-OP** — `StepInfos.tsx` appelle déjà `getDeclarationVeraciteText()` sans argument (cf. PR #108 commit `528bded`), l'UI suit automatiquement la version courante, aucun edit JSX nécessaire ; (d) appliquer la politique re-coche définie par T-288. Articulation T-288, T-292. Chantier d'origine : T-241.
- **T-294 [Technique] Surveiller la surcharge de `completeOnboardingAction`** (issu rapport technique comité T-241 round 2, 04/05/2026) — la server action porte maintenant deux responsabilités : création initiale (token wizard) ET reprise/édition partielle Phase 4 (token vide → producer draft existant) ET appel RPC update_producer_onboarding. À la limite du sur-chargement. Si une vraie page d'édition producteur arrive plus tard (chantier T-289), prévoir l'extraction d'une fonction métier réutilisable autour de la RPC plutôt que de dupliquer la logique de validation+RPC dans deux server actions distinctes. Articulation T-289. Chantier d'origine : T-241.

#### Follow-ups comité review T-241 (round 3, 04/05/2026)

> Rappel r3 : sur la branche, le round a appliqué (a) commentaires MIROIR explicites des deux côtés du couple SQL/JS (`lib/producers/declaration-veracite.ts` au-dessus de `shouldPersistDeclarationVeracite` ; migration T-241 au-dessus du `v_persist :=`), pointant chacun l'autre fichier et imposant la modif jumelle ; (b) clarification du SCOPE ARCHITECTURAL en tête de la RPC `update_producer_onboarding` — désormais passage obligé pour TOUT l'UPDATE onboarding (12 champs business + 3 enums + 3 colonnes T-241), pas uniquement les 3 colonnes T-241 ; (c) documentation explicite de la COUVERTURE INDIRECTE du bloc CASE WHEN SQL via le helper miroir Vitest, en l'absence d'infra de test d'intégration SQL réel — risque connu de divergence silencieuse mitigé par les commentaires MIROIR + ouverture du TODO T-296.

- **T-295 [Sécurité] Audit pré-Live de la fonction `update_producer_onboarding` : SECURITY DEFINER + search_path durci + REVOKE/GRANT EXECUTE** (issu rapport sécurité comité T-241 round 3, 04/05/2026) — vérifier explicitement avant Live que la RPC : (a) reste en `SECURITY DEFINER` (déjà posé par la migration T-241), (b) a son `SET search_path` durci à `public, pg_temp` (et non juste `public` — sinon vulnérable à des objets injectés dans le search_path par défaut du caller), (c) a un `REVOKE EXECUTE ... FROM public, anon` posé via migration de durcissement, et (d) un `GRANT EXECUTE ... TO service_role` (et uniquement service_role — pas `authenticated`, sinon un producteur authentifié pourrait appeler la RPC directement avec ses propres params et forger ses `declaration_indicateurs_*`, détruisant la valeur probante DGCCRF). Articulation T-287 (RLS lecture) — ce point T-295 est le complément côté write/RPC. Chantier d'origine : T-241.
- **T-296 [Technique] Mettre en place une infra de test d'intégration SQL contre Supabase** (issu rapport technique comité T-241 round 3, 04/05/2026) — aujourd'hui le bloc CASE WHEN de la RPC `update_producer_onboarding` (et toute logique métier déplacée en SQL à l'avenir) est couvert INDIRECTEMENT via un helper miroir JS testé par Vitest. Risque connu : divergence silencieuse SQL ↔ JS sur des subtilités (`jsonb ->> 'k'` qui renvoie text/null, `IS DISTINCT FROM` vs strict inequality JS). Cadrer une infra dédiée : (a) Supabase local CLI (`supabase start`) en CI + scripts Vitest qui exécutent la RPC en vrai contre une DB Postgres jetable, OU (b) pg_tap dans `supabase/tests/` exécuté par `supabase test db`. Convention à figer en parallèle : « toute logique métier déplacée en SQL doit avoir un test d'intégration contre Supabase, pas seulement un miroir JS unitaire ». Chantier d'origine : T-241.
- **T-297 [Technique] Convention hygiène migrations : idempotence + `CREATE OR REPLACE` systématique pour les fonctions** (issu rapport technique comité T-241 round 3, 04/05/2026) — la migration T-241 utilise déjà `create or replace function` (donc ré-exécutable sans casse côté fonction). À formaliser dans `docs/METHODOLOGY.md` §Migrations DB comme convention générale projet : (a) toute fonction Postgres doit utiliser `CREATE OR REPLACE`, (b) tout `ALTER TABLE ADD COLUMN` doit utiliser `IF NOT EXISTS` quand pertinent, (c) tout `CREATE TABLE` doit utiliser `IF NOT EXISTS`. Objectif : ré-exécuter une migration deux fois ne casse jamais — utile pour les hot-fix et la reprise post-incident. Chantier d'origine : T-241.

#### Follow-ups comité review BL-2 (round 4, 05/05/2026)

- **T-298 [Technique — repo orchestrateur, pas TerrOir] Refaire passer le test BL-1 réel (bouton « Lancer ce chantier » avec l'item BL-1)** (issu rapport technique comité BL-2 round 4, 05/05/2026) — observation BL-1/BL-2 : le bouton « Lancer ce chantier » de l'orchestrateur a chargé le payload BL-2 alors que le titre du chantier transmis disait « BL-1 ». Cause à investiguer côté orchestrateur (mauvaise propagation de l'ID backlog dans le payload chantier). Une fois la cause corrigée côté orchestrateur, refaire le smoke test BL-1 (bouton « Lancer ce chantier » avec l'item BL-1 sélectionné) pour valider que le titre et le contenu du chantier transmis sont cohérents. Pas de chantier code TerrOir, action côté orchestrateur. Chantier d'origine : BL-2.

### Investigations produit (à trancher)

- **T-150 Consumer cancel route** — la route `/api/orders/[id]/cancel/route.tsx` interdit aujourd'hui au consumer d'annuler sa propre commande (403). Voulu (philosophie anti-abus) ou trou (oubli) ? Si décision = autoriser, ajouter check `session.id === order.consumer_id`. Le test D1 (commit `280ff69`) deviendra un FAIL volontaire qui guidera le fix. Cf rapport TC inspection cancel route 27/04.
- **T-151 Transition `ready → refunded` illégale** — fallback à `cancelled` via `canTransition()` dans cancel route (lignes 97-99). Décision produit à prendre : doit-elle être légale ? Cas concret : un client demande remboursement après que le producer a marqué la commande prête à retirer mais avant le retrait effectif. Implique modif `lib/orders/stateMachine.ts` + tests + handler. Cf rapport TC commit `f57d5ad`.
- **T-152 Aligner guards `canTransition` vs `isTerminal`** — asymétrie API state machine : `canTransition` tolère statut invalide via `?.` (ligne 27), `isTerminal` accès direct (ligne 47, crasherait sur statut invalide). Soit garder l'asymétrie volontaire avec commentaire JSDoc explicatif, soit ajouter un guard. Cf rapport TC commit `f57d5ad`.
- **T-153 Confirm route sans garde rôle explicite** — asymétrie vs cancel route : un admin non-owner d'aucun producer ne peut pas confirmer au nom d'un producer absent. Voulu (philosophie séparation des rôles) ou trou ? Cf rapport TB commit `81b3c1a`.

## 🗺️ Roadmap produit (vision Avril 2026)

> Feuille de route définie le 22/04/2026. 3 niveaux de priorité. Chaque item = une fonctionnalité produit à scoper techniquement le moment venu.

### Priorité HAUTE (prochaines semaines)

> Item original « Prix GMS sur chaque fiche produit » recadré en chantier "Notre démarche" (page pédagogique GMS). Voir section 🟠 En cours en haut. Item retiré de la roadmap.

- **T-200 Score carbone & bien-être animal**
  Sur la page producteur : km parcourus vs moyenne GMS (~1500 km), mode d'élevage (plein air/bâtiment), alimentation, densité. Remplis par le producteur à l'onboarding.
  *Impact : transparence concrète, argument écologique mesurable sans jargon de label.*
  (Onboarding producteur · Page producteur publique)

### Priorité MOYENNE (prochain trimestre)


- **T-221 Schéma interactif circuit court vs GMS**
  Infographie animée sur `/comment-ca-marche` montrant parcours d'un morceau GMS (éleveur → abattoir → transporteur → centrale → GMS → consommateur) vs TerrOir (éleveur → TerrOir → consommateur). Impact sur prix et rémunération éleveur.
  *Impact : argument de conversion puissant, rend concret l'avantage du circuit court.*
  *Articulation : réutilise le composant `<CircuitVisualizer>` produit par Phase C du chantier "Notre démarche". Une fois Phase C livrée, ce schéma `/comment-ca-marche` peut être un montage allégé du visualizer principal.*
  (Page `comment-ca-marche` · Marketing)

- **T-222 D'où vient ma viande**
  Page confirmation + historique commandes : mini-carte du trajet exploitation → point de retrait avec km. Comparaison avec moyenne GMS (1500 km).
  *Impact : moment émotionnel fort après achat, renforce satisfaction et fidélisation, potentiel partage social.*
  (Page confirmation · Historique commandes · Carte)

- **T-223 Calculateur d'impact à la confirmation**
  Sur page confirmation : « Merci. Grâce à vous, Julien a gagné X€ de plus qu'en circuit classique. » Calculé depuis montant commande et taux moyen rémunération éleveur en circuit long (~30%).
  *Impact : crée sentiment de participation et de sens, fidélise au-delà du simple achat.*
  *Articulation : réutilise les ratios de marge fixés dans Phase C du chantier "Notre démarche" (référence chiffrée commune).*
  (Page confirmation · Impact social)

### Priorité BASSE (second semestre 2026)

- **T-240 Compteur impact global plateforme**
  Home + `/a-propos` : « Depuis le lancement, les éleveurs TerrOir ont gagné X€ de plus qu'en circuit classique. » Calcul automatique depuis commandes en base.
  *Impact : argument de marque fort, dimension collective et militante à chaque achat.*
  *Articulation : réutilise les ratios de marge fixés dans Phase C du chantier "Notre démarche".*
  (Home · Page à-propos · Marketing)

- **T-241 Abonnement panier mensuel**
  Commande récurrente chez un éleveur. Paiement auto, notification avant débit, pause/annulation. Producteur voit ses abonnés.
  *Impact : revenus récurrents, fidélisation max. Nécessite travail juridique CGV.*
  (Stripe recurring · Dashboard producteur · CGV)

- **T-242 Carte cadeau & fidélité**
  Carte cadeau TerrOir (crédit en euros, utilisable chez n'importe quel éleveur). Dans un 2e temps : système points de fidélité (X points/€ dépensé, convertibles en réduction).
  *Impact : levier d'acquisition et de rétention.*
  (Stripe · Système de points · Acquisition)

- **T-243 Glossaire du terroir**
  Pages expliquant labels (Label Rouge, AB, AOC…), races (Charolais, Maine-Anjou…), modes d'élevage. Contenu evergreen SEO.
  *Impact : SEO long terme, éducation consumer, autorité éditoriale terroir sarthois.*
  (SEO · Contenu · Pages statiques)

## 🗺️ Vision funnel producteur — Phase 3 (DROP COLUMN `prenom_affichage`)

> Phase 3 finale du chantier "vision funnel producteur" (refonte cohérence admin leads / producteurs décidée 24/04). Phases 1, 2, 2bis et sous-chantier `reads` déjà livrés — voir `CHANGELOG.md`.
>
> Décision : réutiliser `users.prenom` directement pour signer le post-it « Conseil de [prenom] » au lieu d'un champ dédié `producers.prenom_affichage`.

### T-300 Plan de migration finale

1. Retirer les écritures `prenom_affichage = 'À compléter'` dans les 3 INSERT runtime : `create-account.ts`, `login-and-upgrade.ts`, `invitation/page.tsx` SSR.
2. Adapter `StepInfos` (ex-`StepEntreprise`) : retirer le champ + validation.
3. Adapter `app/(producer)/onboarding/page.tsx` : retirer le champ d'édition.
4. Mettre à jour les seeds + cleanup-seed (retirer les writes `prenom_affichage`).
5. Migration SQL : DROP NOT NULL puis DROP COLUMN `producers.prenom_affichage`.
6. Purger le fallback `producer.prenom_affichage` dans `getProducerDisplayName` (le helper devient un simple read sur `users.prenom`).
7. Tests à refresh.

### Ordonnancement

**Prioriser après les bloquants lancement restants** (bascule Stripe Live, onboarder Julien).

## 🔵 Idées / améliorations

- Notation/reviews producteurs (cadre existant via reviews mais flow à valider).
- Export comptable consommateurs + producteurs.
- Gestion des litiges (retrait non effectué, marchandise abîmée).
